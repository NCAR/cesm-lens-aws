plugins:
  source:
    - module: intake_xarray

sources:

  ice_area_aggregate_20C:
    description: 20C monthly ice area (aggregate)
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_sh/monthly/cesmLE-20C-aice.zarr"
      storage_options:
        anon: True
      consolidated: True

  grid_cellmean_ice_thickness_20C:
    description: 20C monthly grid cellmean ice thickness
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_sh/monthly/cesmLE-20C-hi.zarr"
      storage_options:
        anon: True
      consolidated: True

  ice_area_aggregate_RCP85:
    description: RCP85 monthly ice area (aggregate)
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_sh/monthly/cesmLE-RCP85-aice.zarr"
      storage_options:
        anon: True
      consolidated: True

  grid_cellmean_ice_thickness_RCP85:
    description: RCP85 monthly grid cellmean ice thickness
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_sh/monthly/cesmLE-RCP85-hi.zarr"
      storage_options:
        anon: True
      consolidated: True
