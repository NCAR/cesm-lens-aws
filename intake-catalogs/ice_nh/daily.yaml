plugins:
  source:
    - module: intake_xarray

sources:

  ice_area_aggregate_20C:
    description: 20C daily ice area (aggregate)
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_nh/daily/cesmLE-20C-aice_d.zarr"
      storage_options:
        anon: True
      consolidated: True

  grid_cellmean_ice_thickness_20C:
    description: 20C daily grid cellmean ice thickness
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_nh/daily/cesmLE-20C-hi_d.zarr"
      storage_options:
        anon: True
      consolidated: True

  ice_area_aggregate_RCP85:
    description: RCP85 daily ice area (aggregate)
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_nh/daily/cesmLE-RCP85-aice_d.zarr"
      storage_options:
        anon: True
      consolidated: True

  grid_cellmean_ice_thickness_RCP85:
    description: RCP85 daily grid cellmean ice thickness
    driver: zarr
    args:
      urlpath: "s3://ncar-cesm-lens/ice_nh/daily/cesmLE-RCP85-hi_d.zarr"
      storage_options:
        anon: True
      consolidated: True
